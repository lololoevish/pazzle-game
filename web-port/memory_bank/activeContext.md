# Active Context

## Текущий фокус

Устранение зависания при запуске игры (черный экран на старте) из-за некорректной обработки асинхронного `preload` в Phaser 3.

## Активные решения

- Папка `memory_bank/` отсутствовала и создана заново.
- Так как `docs/README.md` отсутствует, первичная синхронизация выполнена по `README.md` и ограниченному обзору кода в `src/`.
- `## Project Deliverables` создан как каноническая markdown-таблица с весами, сумма которых равна 100.
- Phaser Scale Manager переведен с `RESIZE` на `FIT`, чтобы базовый мир 800x600 масштабировался целиком и сохранял aspect ratio на разных мониторах.
- CSS canvas больше не растягивает игру принудительно на `100vw x 100vh`; ограничение идет через `max-width`/`max-height` с поддержкой `100dvh`.
- Переработан `BootScene.ts`: убран асинхронный `preload`, ломавший жизненный цикл Phaser 3. Манифест теперь безопасно загружается как встроенный JSON-ресурс, а постановка файлов в queue и запуск загрузчика вынесены в `create` с проверкой на наличие изображений.
- Решена проблема с повторным запуском пазлов: все ссылки на Phaser-объекты сцены (`player`, `solids`, `lever`, `statusText`, `focusRing` и клавиши управления) теперь принудительно сбрасываются в `undefined` внутри `init()` / `create()` при каждом перезапуске. Это полностью предотвращает ошибки обращения к уничтоженным в предыдущих сессиях объектам и гарантирует безотказный старт сцен.
- Устранена циклическая ошибка мгновенного выхода из 2-й пещеры (и последующих не-лабиринтных пещер): ранее в не-лабиринтах игрок спавнился на координатах `92, 468`. Левый край спрайта игрока шириной 36px заходил на `exitZone` (которая идет по координатам `72, 468` шириной 28px, т.е. до X=86). Это приводило к мгновенному переходу обратно в город в первом же кадре. Спавн перенесен вправо на безопасную позицию `140, 468`, что гарантирует нормальный вход в любую пещеру.
- Удалены мешающие внутренние стены посреди комнат пещер: из метода `createRoomCollision()` убраны 3 старые внутренние перегородки, которые накладывались на интерактивные зоны пазлов (например, на сетку букв) и стесняли движение игрока. Пространство пещеры теперь полностью свободно и удобно для прохождения.
- Решена проблема с повторным запуском пазлов: все ссылки на Phaser-объекты сцены (`player`, `solids`, `lever`, `statusText`, `focusRing` и клавиши управления) теперь принудительно сбрасываются в `undefined` внутри `init()` / `create()` при каждом перезапуске. Это полностью предотвращает ошибки обращения к уничтоженным в предыдущих сессиях объектам и гарантирует безотказный старт сцен.
- Устранена циклическая ошибка мгновенного выхода из 2-й пещеры (и последующих не-лабиринтных пещер): ранее в не-лабиринтах игрок спавнился на координатах `92, 468`. Левый край спрайта игрока шириной 36px заходил на `exitZone` (которая идет по координатам `72, 468` шириной 28px, т.е. до X=86). Это приводило к мгновенному переходу обратно в город в первом же кадре. Спавн перенесен вправо на безопасную позицию `140, 468`, что гарантирует нормальный вход в любую пещеру.
- Удалены мешающие внутренние стены посреди комнат пещер: из метода `createRoomCollision()` убраны 3 старые внутренние перегородки, которые накладывались на интерактивные зоны пазлов (например, на сетку букв) и стесняли движение игрока. Пространство пещеры теперь полностью свободно и удобно для прохождения.
- Ликвидировано повторение порядка головоломок: общее количество пещер увеличено до **36 уровней**, а список уникальных типов головоломок расширен до **14 штук**. Для уровней 15-36 внедрена большая нелинейная перетасованная последовательность `SHUFFLED_ORDER`, исключающая линейную повторяемость.
- Добавлено 2 совершенно новых уникальных типа головоломок:
  - **Магическая арифметика (`math_puzzle`):** На стене генерируется случайное уравнение (сложение, вычитание или умножение). Игрок должен решить его и наступить на соответствующую плиту с правильным числом на полу.
  - **Световые кристаллы (`simon_says`):** На стене размещаются 4 цветных кристалла, которые вспыхивают в определенном порядке. Игрок должен подойти и коснуться их (нажать `E`) в той же последовательности.
- Полностью устранено повторение содержимого внутри головоломок:
  - **Лабиринты:** Создано 3 различных варианта карт лабиринтов (`MAZE_GRID_1`, `MAZE_GRID_2`, `MAZE_GRID_3`), которые выбираются на основе уровня, делая прохождение пещер-лабиринтов разнообразным.
  - **Поиск слов:** Подготовлено 3 различных варианта буквенных сеток и наборов слов (включая новые слова «КРИСТАЛЛ», «ЗОЛОТО», «ПОРТАЛ», «ГЕРОЙ», «ЗАГАДКА», «ПОБЕДА», «РЫЧАГ», «КАМЕНЬ»), которые меняются от уровня к уровню.
  - **Загадки:** Создан обширный пул из 8 мудрых загадок, из которого при старте уровня выбираются случайные 3 загадки, а варианты ответов динамически перетасовываются с добавлением ложного ответа.
  - **Звуковые ловушки, Песнь пещер и Прыгающий путь:** Последовательности нот, рун и безопасных плит теперь полностью генерируются случайным образом при каждом входе в пещеру, делая головоломки уникальными.
- Внедрен ультра-сочный и динамичный геймплей:
  - **Рывок странника (Dash):** При нажатии на `Space` в хабе и пещерах ГГ совершает резкий рывок вперед с троекратным ускорением, спавном красивого шлейфа призрачных силуэтов (`ghost trail`), выбиванием облака пыли из-под ног и эффектом тряски камеры.
  - **Система Squash & Stretch:** Персонаж больше не «плывет» безжизненно. При ходьбе его попиксельный спрайт плавно покачивается и деформируется по синусоиде, а при прыжках растягивается. В состоянии покоя ГГ плавно совершает «дыхательные» колебания.
  - **Пиксельный пылевой шлейф:** Движение ГГ сопровождается динамичным разлетом микроскопических пиксельных пылинок, затухающих в воздухе.
  - **Двойной прыжок (Double Jump):** В платформере ГГ теперь умеет совершать двойной прыжок в воздухе. При повторном прыжке ГГ забавно сжимается, камера делает легкий импакт-встряску, а вниз летит пучок пылевых частиц!
- Создан ослепительный честный пиксель-арт:
  - Написаны вспомогательные попиксельные хелперы `drawPixelPattern()`, `drawPixelCircle()` и `addPixelNoise()`, рисующие блоки строго без сглаживания.
  - Разработаны полноценные ретро-сетки попиксельного рисования (с размером виртуального пикселя 2x2).
  - **Главный герой (Player):** Полностью перерисован. Вместо пингвинообразного вида он получил облик таинственного рыцаря-странника в глубоком синем капюшоне со светящимися во тьме голубыми глазами, стальных доспехах, с золотой пряжкой на груди и роскошным алым плащом за спиной.
  - Попиксельно нарисованы Староста Иара (посох со сферой), Механик Роан (куртка, жилет, гаечный ключ), Архивариус Тель (мантия, монокль, летающая книга), 3D Кристалл и Напольный рычаг.
  - Добавлен аутентичный пиксельный ретро-шум на фоновые текстуры города и пещер (`townBackground` и `caveBackground`), что создает настоящую 16-битную атмосферу.
  - Для всех процедурных текстур жестко задана фильтрация `NEAREST`. В конфиге игры задействованы `antialias: false` и `roundPixels: true`. Графика теперь идеально пиксельная и резкая на любом мониторе.
  - **Игрок:** Нарисован стильный струящийся фиолетовый плащ за спиной, красивый градиент тела для 3D-объема, глаза с бликами и зрачками, а также золотой нагрудный амулет.
  - **Староста Иара:** Добавлена детальная седая борода с прядями, а в руку вложен длинный посох с золотым навершием и ярко светящейся синей сферой с бликами.
  - **Механик Роан:** Одет в кожаную куртку с защитным нагрудником, на лбу красуются круглые защитные очки с линзами, а за плечом детально нарисован серебряный гаечный ключ.
  - **Архивариус Тель:** Наряжен в мистическую мантию с ручной золотой вышивкой, на левый глаз добавлен изящный монокль на цепочке, а рядом левитирует открытая волшебная книга с пожелтевшими страницами.
  - **Кристалл:** Переработан в полноценный 3D-ромб с четырьмя гранями, центральным светящимся неоновым ядром и сияющими искрами вокруг.
  - **Рычаг:** Размещен на резном каменном постаменте с выбитой светящейся руной, получил металлическую ось, деревянную рукоять и ярко-красный неоновый шар-набалдашник с сильным свечением.
  - **Арка портала (Вход):** Обзавелась массивными каменными колоннами с рельефными булыжниками и швами, светящимися бирюзовыми неоновыми символами-рунами и загадочным фиолетово-синим порталом внутри.
  - **Рунический круг (magicCircle):** Превратился в сложную геометрическую пентаграмму с внешним и внутренним руническим кольцом, вписанной пятиконечной звездой, лучами и светящимися магическими точками.
  - **Закатные облака (softCloud):** Получили красивый многослойный объемный градиент с переливом от розоватого до нежно-голубого и лавандового с мягким золотистым бликом уходящего солнца.
  - **UI Панели (uiPanel):** Добавлена красивая текстура холста внутри рамки и золотые заклепки по всем четырем углам.
  - **Кнопки (button):** Получили приятный объемный градиент и каемку с тенью снизу, усиливающую ощущение глубины.
  - **Стены пещеры (caveWall):** Каменные перегородки обзавелись глубокой фактурой, трещинами и мелкими светящимися бирюзовыми частицами кристаллов.
  - **Трава (grassPatch):** Нарисованы более густые травинки с нежными цветками.

## Приоритеты

1. Поддерживать `memory_bank/projectbrief.md` как канонический источник процента готовности проекта.
2. При изменениях scope немедленно обновлять deliverables и проверять сумму Weight.
3. При архитектурных изменениях синхронизировать документацию проекта.
