# Adventure Puzzle Game - Active Context

**Дата последнего обновления**: 28 апреля 2026
**Текущая версия**: 1.3.0-dev
**Статус**: В разработке, основной исполняемый и продуктовый фокус закреплён за GameMaker Studio 2 версией

## Текущий фокус

1. Доводить `pazzle-game-gamemaker/` до состояния реально проходимой GMS2-игры без ручной правки прогресса и runtime-логики.
2. Устранять legacy-расхождения между старой 6/12-уровневой моделью и текущей 24-уровневой структурой уровней, пазлов и сохранений.
3. Держать `memory_bank/` и `docs/README.md` синхронизированными с реальным состоянием кода, а не с историческим статусом Rust-ветки.
4. Продолжать runtime-полировку game feel, аудио и VN-подачи без наращивания архитектурного долга.
5. В работу взята унификация `scr_game_state.gml` и `scr_save_system.gml` под единую 24-уровневую модель прогресса и сброса.
6. В работу взята актуализация `docs/gms2_testing_strategy.md`, `docs/gms2_unit_test_scenarios.md` и `test_scenarios/` под реальный GMS2 API.
7. Выполнена повторная синхронизация `memory_bank/` по актуальным правилам удалённого `AGENTS.md`: проверены `Project Deliverables`, канонические статусы и сумма весов `100`.
8. Выполнен точечный аудит GMS2 runtime на legacy-хардкоды количества уровней: условия достижений и межуровневых переходов больше не завязаны на фиксированные `6`/`12`, а читают длину `global.game_progress.levels`.
9. В работе выполнен runtime-аудит GMS2 API-совместимости: исправлены невалидные helper-вызовы, конфликт имён переходов, небезопасные обращения к optional global-переменным и некорректные формы поиска комнат/ресурсов.
10. Выполнена синхронизация Markdown-документации: root README, `pazzle-game-gamemaker/README.md`, `current_task.md`, модульные заметки Memory Bank и точечные GMS2-документы больше не описывают Rust/Python/6-уровневую модель как актуальный продуктовый scope.
11. Выполнена текущая проверка по актуальному удалённому `AGENTS.md`: локальные инструкции синхронизированы, `memory_bank/projectbrief.md` содержит обязательную таблицу `## Project Deliverables` с колонками `ID | Deliverable | Status | Weight`, каноническими статусами и суммой весов `100`.
12. В работу взята стабилизация save/load на повреждённые `.sav`, fallback через `.ini` и частично заполненные структуры сохранения; добавлены нормализация прогресса и тестовые сценарии для этих случаев.

## Что подтверждено в текущем коде

1. Основной рабочий код проекта сосредоточен в `pazzle-game-gamemaker/`.
2. `scr_puzzle_manager.gml` уже поддерживает 12 базовых типов пазлов; целевой маршрут расширен до 24 пещер.
3. `obj_player.gml` реализует двухрежимное движение `topdown`/`platformer` и уже содержит буфер прыжка, coyote time и variable jump height.
4. `obj_npc.gml` сочетает побочные мини-игры, реактивные состояния NPC и интеграцию VN-системы.
5. `scr_audio_manager.gml` уже включает музыку по сценам, SFX, контекстные модификаторы громкости и систему приоритетов музыки.
6. В `scr_game_state.gml` уже убраны жёсткие проверки на 6/12 уровней для статуса экспедиции, общего прогресса и открытия следующих пещер; логика переведена на общий `GAME_LEVEL_COUNT = 24`.
7. В `scr_save_system.gml` загрузка, инициализация, reset и конвертация сохранений теперь ориентированы на `SAVE_LEVEL_COUNT = 24` и умеют безопасно расширять массив уровней.
8. Дополнительно синхронизированы `scr_ui_manager.gml`, `obj_game_manager.gml`, `scr_audio_manager.gml` и `obj_player.gml`: прогресс HUD, игровые state-cases и проверки доступности уровней больше не ограничены старой 6-уровневой моделью.
9. `docs/gms2_testing_strategy.md` и `docs/gms2_unit_test_scenarios.md` требуют дальнейшей синхронизации под 24-уровневую прогрессию после расширения маршрута.
10. `test_save_system.gml` переведён на 24 уровня; остальные тестовые сценарии ещё требуют ревизии под новый маршрут.
11. `scr_achievement_manager.gml` использует helper-функции для подсчёта половины маршрута и полного завершения по фактическому числу уровней.
12. `scr_level_transition_platformer.gml` определяет последний уровень через текущую модель прогресса, а не через литерал `12` в условии перехода.
13. `scr_init_globals.gml`, `scr_save_system.gml`, `scr_asset_manager_master.gml`, `scr_resource_manager.gml`, `scr_sprite_loader.gml`, `scr_interlevel_platformer.gml`, `scr_audio_manager.gml`, `scr_ui_manager.gml`, `scr_vn_portrait_manager.gml`, `obj_player.gml` и `obj_game_manager.gml` очищены от подтверждённых runtime-рисков вокруг несуществующих helper-функций и необязательных globals.
14. `memory_bank/systemPatterns.md`, `memory_bank/modules/entities.md`, `memory_bank/modules/utils.md`, `memory_bank/modules/scenes.md` и `memory_bank/other/legacy.md` переписаны под текущую GMS2-архитектуру и legacy-reference статус Python/Rust материалов.
15. Маршрут расширен до 24 уровней: обновлены константы прогресса/сохранений, добавлены config-комнаты 13-24 и sprite asset configs с говорящими именами `spr_caveNN_<theme>_<role>_<detail>`.

## Активные решения

### Архитектура

1. `docs/README.md` считается единственным источником правды по верхнеуровневой архитектуре.
2. GMS2-версия считается основной продуктовой веткой; Rust-ветка сохраняется как референс маршрута и прошлых реализаций.
3. Статус deliverables должен отражать фактическую готовность runtime-маршрута, а не только наличие файлов или планов миграции.
4. Любая новая работа по прогрессу, сохранениям, тестированию или маршрутам должна исходить из 24-уровневой модели.

### Приоритеты

1. Стабилизировать модель прогресса и сохранений.
2. Актуализировать документацию и тестовую стратегию.
3. Потом продолжать контентную и сценическую полировку аудио, VN и пещер.

## Известные проблемы и ограничения

1. Ключевые документы Memory Bank синхронизированы под GMS2, но часть старых справочных документов вне Memory Bank всё ещё может быть historical reference.
2. Основные runtime-допущения о количестве уровней в прогрессе, сохранениях и переходах выровнены под 24-уровневую модель; остаются исторические формулировки в части справочной документации и valid reference к 12 базовым типам пазлов.
3. GML-скрипты крупных систем пока остаются достаточно монолитными, что повышает риск регрессий.
4. Автоматический линтинг `.gml` отсутствует; проверка качества опирается на чтение кода, grep-аудит, сценарии тестирования и ручной runtime-проход.

## Следующие шаги

1. При возможности прогнать обновлённые GML-тесты в runtime GameMaker и зафиксировать фактический результат smoke/regression-прохода.
2. Проверить новые сценарии save/load в runtime GameMaker: corrupted `.sav` + fallback `.ini`, partial save defaults и legacy expansion.
3. Продолжить аудит save/load на сохранения с некорректными типами полей и старые достижения.
4. Проверить оставшиеся root/docs historical reference файлы и при необходимости добавить явные пометки об их архивном статусе.
